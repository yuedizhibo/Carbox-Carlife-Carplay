add_library(carplay_forward src/wireless_endpoint.cpp src/carplay_forward.cpp src/catplay_capture.cpp)
target_compile_features(carplay_forward PUBLIC cxx_std_20)
target_include_directories(carplay_forward PUBLIC include)
target_link_libraries(carplay_forward PUBLIC session_core wired_carplay_output synthetic_wireless)
if(NOT MSVC)
  target_compile_options(carplay_forward PRIVATE -Wall -Wextra -Wpedantic)
endif()
add_executable(carplay_forward_tests tests/forward_tests.cpp)
target_link_libraries(carplay_forward_tests PRIVATE carplay_forward)
add_test(NAME carplay_forward_tests COMMAND carplay_forward_tests)
set_tests_properties(carplay_forward_tests PROPERTIES TIMEOUT 30)
